plugins {
    id 'com.bmuschko.docker-remote-api' version '4.8.0'
}

// Import task types
import com.bmuschko.gradle.docker.tasks.image.*

// Use task types
task buildDemonstrationImage(type: DockerBuildImage) {
    inputDir = file('./')
    tags.add('dcos-demonstrations:latest')
}
